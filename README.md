# Lapce Ruby (LanguageServer)

## Lapce plugin for the Ruby language powered by solargraph

## Available configuration

```toml
[lapce-solargraph]
serverPath = "path/to/solargraph"
serverArgs = ["stdio"]
formatting = true
diagnostics = true
```